# Clippy

Clippy is a VS Code extension that shows a `Saved!` popup every time a file is saved.

## Usage

Open this folder in VS Code and press `ctrl+shift+D` to run the extension in an Extension Development Host. Press `Ctrl+S` in the development host to see the popup.
